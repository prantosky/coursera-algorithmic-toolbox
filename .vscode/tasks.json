{
	"version": "2.0.0",
	"tasks": [
		{
			"type": "shell",
			"label": "Linux: g++ build active file",
			"command": "/usr/bin/g++",
			"args": [
				"-g",
				"-std=c++1z",
				"${file}",
				"-o",
				"${fileDirname}/${fileBasenameNoExtension}.out"
			],
			"options": {
				"cwd": "/usr/bin"
			},
			"problemMatcher": [
				"$gcc"
			],
			"group": "build"
		}
	]
}